# 디자인 리서치 2단계 정리 — 전원 넣기 전 부품 통합 카드 에이전트

작성일: 2026-09-14
근거: `design_research.md`(세부화판)
목적: 리서치 결과를 서비스 목적·사용자·맥락에 맞게 다시 묶어, 이후 디자인 계획에 쓰기 쉬운 형태로 정리한다.

## 1. 다시 보는 서비스 목적(리서치 해석의 기준)

이 서비스는 다음 목적을 가진다.

- 실제 장착·전원 인가 전에 IC와 캐리어를 분리해 식별하고, 전기 호환성을 근거 기반으로 판정한다.
- 판정 자체보다 **판정 근거가 오해 없이 보이게 하는 것**이 더 중요하다.
- 카드는 한 번 쓰고 버리는 결과가 아니라, 저장·재호출·재사용 가능한 정리 결과로 남아야 한다.

리서치는 이 목적에 맞는 요소만 채택해야 하며, "예쁘게"나 제품/서비스 성숙도 같은 넓은 기준으로 서비스 방향을 바꾸지 않는다.

## 2. 사용자·맥락 관점에서 읽는 리서치

### 2.1 사용자 특성에서 나오는 필요

- 사용자는 부품명 하나만 보고 시작하는 경우가 많다. 그래서 입력과 출력 모두에서 IC/캐리어/타겟 보드 분리를 자연스럽게 유도해야 한다.
- 관찰 사실은 짧아 시작하므로, 사진·핀·수·커넥터·전원·구매 링크 같은 최소한의 정보로 검토를 시작할 수 있어야 한다.
- 외관이 닮았다는 이유만으로 전압·핀맵을 가져오는 습관이 생기기 쉬우므로, 그 유혹을 화면에 명시적으로 막아야 한다.

### 2.2 사용 맥락에서 나오는 필요

- 실제 연동 전에는 시뮬레이션/예시로 계약을 먼저 확인한다. 따라서 모드·예시·실제 응답 구분이 초반부터 분명해야 한다.
- 카드는 저장·재사용·누적 흐름을 타므로, 한 응답의 상태가 다음 응답에서 완전히 지워지지 않는 감각을 유지해야 한다.
- 웹 검색 UI를 바로 넣기보다 부품 테이블/근거 중심으로 대체하는 현재 방침을 유지한다. 저작권 이슈는 별도 확인 대상이다.

## 3. 리서치 요소를 목적 축으로 다시 읽기

### 3.1 신뢰·출처·근거 추적

- 핵심 역할: 확인되지 않은 값을 확정된 결과처럼 보이지 않게 하고, 값이 어디서 왔는지 추적 가능하게 남긴다.
- 서비스 연결점:
  - [확인된 자료]는 출처·문서 버전·target·확인 날짜를 함께 남기는 자리
  - 확인/계산/미확인/planned 구분은 화면에서도 구분 가능해야 함
  - 출처 충돌은 5열 표로 남기고, 자동 해소하지 않음
  - 이전 증거 무효화·차단 해제·재검증 이력은 삭제하지 않고 남기는 방향

### 3.2 상태 표시와 오해 방지

- 핵심 역할: 상태·배지·단계·모드가 서로 다른 신호를 보내지 않게 한다.
- 서비스 연결점:
  - PROVISIONAL/BLOCKED/VERIFIED는 색상+레이블로 구분
  - 상태 바에서 [카드 N/5]와 상태 문구 우선 노출
  - BLOCKED일 때 현재 차단 항목이 비어 있지 않으면 눈에 띄게 표시
  - 단계 전제만 짧게 보여 주고, 미확정 단계 후보를 함부로 노출하지 않음
  - 시뮬레이션/예시/실제 응답을 구분 가능한 상태로 유지

### 3.3 정보 구성과 선택적 확장

- 핵심 역할: 상태 바는 항상 보이고, 구역별 상세는 필요할 때만 열게 한다.
- 서비스 연결점:
  - 7구역 접기/펼치기 유지, 단 안전 관련 핵심 정보는 과잉 은닉하지 않음
  - 기술 사용자에게 필요한 정보 밀도는 유지하되, 게이트·출처·충돌이 한눈에 파악될 수 있도록 항목을 나눔
  - 접기/펼치기는 키보드/스크린리더 대응이 가능한 방향으로 유지

### 3.4 판정 구조와 게이트 표현

- 핵심 역할: 전기 호환성 판정은 항목별 게이트로 보이고, 통과/미통과 요약과 차단·충돌 수를 드러낸다.
- 서비스 연결점:
  - 전원·전류·로직 레벨·극성·GND·핀·버스 게이트를 항목별로 표시
  - 각 항목은 VERIFIED/계산/미확인/BLOCKED로 구분
  - 출처 충돌은 5열 표로 노출하고 해제 조건을 함께 표시
  - 전류 게이트는 일반 소비 전류와 최대·피크 전류를 나누고 보드 공급 한계와 연결
  - 레벨 충돌 의심·극성 미확인 시 배선·전원 행동을 바로 주지 않는다는 계약 의도 유지

### 3.5 입력·출력 구분(식별성)

- 핵심 역할: 입력 힌트와 예시, 출력 구역 모두에서 IC·캐리어·타겟 보드 분리를 유지한다.
- 서비스 연결점:
  - 입력 힌트는 "부품 이름만"보다 분리를 자연스럽게 권장
  - 입력 예시는 부품 분리 감각을 보여 주는 방향
  - [식별 상태]는 IC·캐리어·타겟 보드를 각각 분리해 보여 줌
  - 외관이 닮았다는 이유만으로 전압·핀맵을 가져오지 않음

### 3.6 저장·재호출·재사용

- 핵심 역할: 저장·불러오기·내보내기가 상태·단계·근거와 함께 이어지게 한다.
- 서비스 연결점:
  - 저장 시 현재 응답(상태 줄, 단계, 상태, 구역 내용) 포함
  - 불러오기 시 폼 입력과 7구역 복원
  - 저장 목록에서 단계·상태·저장 시각 표시
  - JSON 내보내기는 카드·응답 구조를 그대로 확인할 수 있게 남김
  - [관찰 사실]은 매 응답 덮어쓰기가 아니라 누적 감각을 갖도록 표현 구조 준비

### 3.7 비교·대안·협업·설명 가능성

- 핵심 역할: 후보/대안은 개수 제한과 근거·출처·상태를 붙이고, 판정은 이유·근거를 함께 보이게 한다.
- 서비스 연결점:
  - 후보는 최대 2개, 각 후보에 근거·출처·상태를 붙인다는 계약 유지
  - 수명주기·유통사 연동은 무리하게 넣지 않음
  - 판정은 인상적 문장보다 근거·출처·상태·해제 조건에 의존
  - 협업 기능 자체는 지금 무리하게 확장하지 않음

## 4. 리서치에서 "유지할 장점"으로 볼 수 있는 것들

아래 요소는 리서치 관점에서도 장점으로 읽히며, 이후 디자인 변경이 있어도 유지·강화 대상이다.

- 상태 바에서 [카드 N/5]와 상태 문구를 먼저 보여 주는 구조
- PROVISIONAL/BLOCKED/VERIFIED를 색상+레이블로 구분하는 배지
- 7구역 고정 순서와 접기/펼치기 구조
- 전기 호환성 게이트 항목별 상태 표시
- 출처 충돌 5열 표와 해제 조건 표시
- 시뮬레이션/예시/실제 응답 구분 표시
- 단계 전제 라벨
- 저장·불러오기·JSON 내보내기에서 상태·단계·근거 이어짐
- [확인된 자료]에 출처·문서 버전·target·확인 날짜를 남기는 구조
- 확인/계산/미확인/planned 구분을 화면에 드러내는 방향
- 이전 증거 무효화·차단 해제·재검증 이력을 삭제하지 않는 방향

## 5. 리서치에서 "주의·별도 확인" 대상

- 웹 검색 UI는 저작권 표시 문제 확인 전에는 넣지 않고, 부품 테이블/근거 중심으로 대체한다.
- 지각 부하·시각적 중요도·디자인 시스템 성숙도 같은 폭넓은 기준은 지금 단계에서 이 서비스의 판단 기준으로 삼지 않는다. 이후 화면 검토 시 참고 요소로만 남긴다.
- 수명주기·유통사 데이터 연동, 협업 기능 확장은 별도 확인 없이 무겁게 넣지 않는다.

## 6. 2단계 정리의 쓰임

이 파일은 리서치 세부(design_research.md)를 서비스 목적·사용자·맥락에 비춰 다시 읽은 결과다. 이후 `design_plan.md`를 정리할 때 "어떤 장점이 왜 장점이 되는지"를 빠르게 연결하는 기준으로 쓴다.
